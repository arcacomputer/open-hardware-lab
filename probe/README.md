# Hardware probe

Status: **planned, not implemented**.

The intended probe will produce a privacy-scrubbed, structured snapshot for repeatable hardware compatibility research. It must fail closed around credentials, serial numbers, MAC addresses, usernames and unrelated local data.

No command, package or release is currently advertised. Implementation claims belong here only after executable code and focused tests exist.
